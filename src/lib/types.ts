export interface CartItem {
	id: string;
	quantity: number;
	image: string;
	name: string;
	price: number;
}
export interface Message {
	title: string;
	text: string;
	type: string;
	timeout: number;
}
